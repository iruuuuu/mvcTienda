<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Pedidos</title>
</head>
<body>
    <a href="index.php">Volver a la tienda</a>

    <h2>Mis Pedidos</h2>

    <?php if (isset($pedidos) && count($pedidos) > 0): ?>
        <ul>
        <?php foreach ($pedidos as $pedido): ?>
            <li>
                Pedido ID: <?php echo htmlspecialchars($pedido->getId()); ?> - 
                Producto ID: <?php echo htmlspecialchars($pedido->getProductId()); ?>
                <?php // Aquí podrías hacer una consulta para obtener el nombre del producto si lo necesitas ?>
            </li>
        <?php endforeach; ?>
        </ul>
    <?php else: ?>
        <p>No tienes pedidos todavía.</p>
    <?php endif; ?>
</body>
</html>